<Window x:Class="LegoClickerCS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lego Clicker Loader"
        Width="300" Height="180"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    
    <Border Background="{DynamicResource BgBrush}" CornerRadius="10" 
            BorderBrush="{DynamicResource PanelBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource PanelBrush}" CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Lego Clicker Loader" Foreground="{DynamicResource TextBrush}" 
                               FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                    <Button x:Name="CloseButton" Content="×" 
                            HorizontalAlignment="Right" Width="30" Height="30"
                            Background="Transparent" Foreground="{DynamicResource DimTextBrush}"
                            BorderThickness="0" FontSize="18" FontWeight="Light"
                            Click="CloseButton_Click" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="0,10,0,0">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E81123"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Content Panel -->
            <StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Center">
                
                <TextBlock Text="Injection Control" Foreground="{DynamicResource TextBrush}" 
                           FontSize="16" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                <Button x:Name="InjectButton" Content="Inject / Connect" Height="35"
                        Style="{StaticResource DarkButton}" Click="InjectButton_Click"
                        FontSize="14" Margin="0,0,0,10" Cursor="Hand">
                 <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                </Button>

                <TextBlock x:Name="InjectionStatusText" 
                           Text="Status: Not Injected" 
                           Foreground="{DynamicResource DimTextBrush}" 
                           FontSize="12" HorizontalAlignment="Center"/>
                           
            </StackPanel>
        </Grid>
    </Border>
</Window>
